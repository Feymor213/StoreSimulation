# Duplicate this file and rename it to .env

# Environment
NODE_ENV=development # Change to 'production' for production use

# Database credentials
# !WARNING! Use secure credentials in production
POCKETBASE_SUPERUSER_EMAIL=admin@admin.com
POCKETBASE_SUPERUSER_PASSWORD=adminadmin
